plugins {
    id 'idea'
    id 'scala'
}

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile group: 'org.scala-lang', name: 'scala-library', version: scalaFullVersion
    compile group: 'org.apache.spark', name: 'spark-core_'+scalaBaseVersion, version: sparkVersion
}
